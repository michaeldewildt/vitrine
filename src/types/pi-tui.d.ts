/** * Ambient declaration for `@earendil-works/pi-tui` — the pi TUI component * library. Same minimal-mirror treatment as pi-coding-agent.d.ts: the * package resolves only under pi's extension loader (jiti aliases it), not * in this repo — so bun tests mock the specifier (src/vitrine.test.ts), and * only TYPE usage from this file is allowed in modules that bun resolves * (done-render.ts keeps its pi imports type-only). * * Surface Vitrine uses (verified 2026-09-18 against the installed 0.85.1 * docs (tui.md "Built-in Components") + * examples/extensions/built-in-tool-renderer.ts): `Text`, `Container`, * `Markdown`. The markdown theme comes from `getMarkdownTheme()` * (pi-coding-agent — typed as `unknown` here to keep this mirror * self-contained). */
declare module "@earendil-works/pi-tui" { /** A pi-tui component (the minimal surface; structural). */ export interface TuiComponent { render(width: number): string[]; dispose?(): void; } /** Multi-line text with word wrapping. */ export class Text implements TuiComponent { constructor(content: string, padX?: number, padY?: number, bgFn?: (s: string) => string); setText(content: string): void; render(width: number): string[]; } /** Groups child components vertically. */ export class Container implements TuiComponent { constructor(); addChild(component: TuiComponent): void; removeChild(component: TuiComponent): void; render(width: number): string[]; } /** Renders markdown with syntax highlighting (theme: `getMarkdownTheme()`). */ export class Markdown implements TuiComponent { constructor(text: string, padX: number, padY: number, theme: unknown); setText(text: string): void; render(width: number): string[]; }
}
